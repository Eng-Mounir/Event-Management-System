<!DOCTYPE html>
<html lang="en" data-bs-theme="light">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard | EventHub</title>
    
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
    
    <style>
        :root {
            --primary: #4361ee;
            --secondary: #3a0ca3;
            --gradient: linear-gradient(135deg, #4361ee 0%, #3a0ca3 100%);
        }
        
        body {
            font-family: 'Inter', sans-serif;
            background-color: #f8fafc;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }
        
        .navbar-brand {
            font-family: 'Poppins', sans-serif;
            font-weight: 700;
            font-size: 1.8rem;
            background: var(--gradient);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }
        
        .hero-section {
            background: var(--gradient);
            color: white;
            padding: 100px 0;
        }
        
        .card {
            border: none;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
            transition: transform 0.3s ease;
        }
        
        .card:hover {
            transform: translateY(-5px);
        }
        
        .btn-primary {
            background: var(--gradient);
            border: none;
            padding: 12px 30px;
            border-radius: 50px;
        }
        
        .btn-primary:hover {
            opacity: 0.9;
        }
        
        .footer {
            background: #212529;
            color: white;
            margin-top: auto;
        }
        
        .navbar {
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
        }
        
        .active {
            color: var(--primary) !important;
            font-weight: 600;
        }
        
        /* Dashboard specific styles */
        .nav-tabs-custom .nav-link {
            border: none;
            color: #6c757d;
            font-weight: 500;
            padding: 0.75rem 1.5rem;
            border-bottom: 3px solid transparent;
        }
        
        .nav-tabs-custom .nav-link.active {
            color: #4361ee;
            border-bottom: 3px solid #4361ee;
            background: transparent;
        }
        
        .timeline {
            position: relative;
            padding-left: 20px;
        }
        
        .timeline::before {
            content: '';
            position: absolute;
            left: 7px;
            top: 0;
            bottom: 0;
            width: 2px;
            background: #e9ecef;
        }
        
        .timeline-item {
            position: relative;
            margin-bottom: 1.5rem;
        }
        
        .timeline-icon {
            position: absolute;
            left: -20px;
            top: 0;
            width: 16px;
            height: 16px;
            border-radius: 50%;
            color: white;
            font-size: 0.5rem;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        
        .timeline-content {
            padding-left: 15px;
        }
        
        .event-icon {
            width: 48px;
            height: 48px;
            display: flex;
            align-items: center;
            justify-content: center;
            border-radius: 10px;
        }
        
        /* Notification badge styles */
        .notification-badge {
            position: absolute;
            top: 0;
            right: 0;
            transform: translate(50%, -50%);
            font-size: 0.7rem;
            min-width: 18px;
            height: 18px;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        
        /* Recent Tickets Styles */
        .recent-ticket-item {
            border-left: 4px solid #4361ee;
            background: #f8f9fa;
            transition: all 0.3s ease;
        }
        
        .recent-ticket-item:hover {
            background: #e9ecef;
            border-left-color: #3a0ca3;
        }
        
        .payment-status-badge {
            font-size: 0.65rem;
            padding: 0.2rem 0.5rem;
        }
        
        .amount-badge {
            font-size: 0.9rem;
            font-weight: 600;
        }
        
        /* Ticket table responsive */
        @media (max-width: 768px) {
            .table-responsive {
                font-size: 0.85rem;
            }
            
            .amount-badge {
                font-size: 0.8rem;
            }
            
            .event-icon {
                width: 36px;
                height: 36px;
            }
        }
    </style>
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-light bg-white sticky-top">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="bi bi-calendar-event me-2"></i>EventHub
            </a>
            
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav mx-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/">
                            <i class="bi bi-house-door"></i> Home
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/events">
                            <i class="bi bi-calendar3"></i> Events
                        </a>
                    </li>
                    <% if (user) { %>
                        <li class="nav-item">
                            <a class="nav-link active" href="/dashboard">
                                <i class="bi bi-speedometer2"></i> Dashboard
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/users/tickets">
                                <i class="bi bi-ticket-perforated"></i> My Tickets
                            </a>
                        </li>
                    <% } %>
                </ul>
                
                <div class="d-flex align-items-center">
                    <!-- Notification Bell -->
                    <% if (user) { %>
                        <li class="nav-item me-3 position-relative list-unstyled">
                            <a class="nav-link position-relative" href="/notifications">
                                <i class="bi bi-bell fs-5"></i>
                                <% if (stats.unreadNotifications && stats.unreadNotifications > 0) { %>
                                    <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger notification-badge">
                                        <%= stats.unreadNotifications %>
                                    </span>
                                <% } %>
                            </a>
                        </li>
                    <% } %>
                    
                    <% if (user) { %>
                        <div class="dropdown">
                            <a href="#" class="d-flex align-items-center text-decoration-none dropdown-toggle" 
                               data-bs-toggle="dropdown">
                                <div class="rounded-circle bg-primary d-flex align-items-center justify-content-center" 
                                     style="width: 40px; height: 40px; color: white; font-weight: 600;">
                                    <%= user.name.charAt(0).toUpperCase() %>
                                </div>
                                <span class="ms-2"><%= user.name %></span>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end">
                                <li><a class="dropdown-item" href="/dashboard">
                                    <i class="bi bi-speedometer2 me-2"></i>Dashboard
                                </a></li>
                                <li><a class="dropdown-item" href="/auth/profile">
                                    <i class="bi bi-person-circle me-2"></i>Profile
                                </a></li>
                                <li><a class="dropdown-item" href="/notifications">
                                    <i class="bi bi-bell me-2"></i>Notifications
                                    <% if (stats.unreadNotifications && stats.unreadNotifications > 0) { %>
                                        <span class="badge bg-danger float-end mt-1">
                                            <%= stats.unreadNotifications %>
                                        </span>
                                    <% } %>
                                </a></li>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item" href="/auth/logout">
                                    <i class="bi bi-box-arrow-right me-2"></i>Logout
                                </a></li>
                            </ul>
                        </div>
                    <% } else { %>
                        <a href="/auth/login" class="btn btn-outline-primary me-2">
                            <i class="bi bi-box-arrow-in-right"></i> Login
                        </a>
                        <a href="/auth/register" class="btn btn-primary">
                            <i class="bi bi-person-plus"></i> Register
                        </a>
                    <% } %>
                </div>
            </div>
        </div>
    </nav>

    <!-- Flash Messages -->
    <% if (success) { %>
        <div class="container mt-3">
            <div class="alert alert-success alert-dismissible fade show" role="alert">
                <i class="bi bi-check-circle-fill me-2"></i>
                <%= success %>
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        </div>
    <% } %>
    
    <% if (error) { %>
        <div class="container mt-3">
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                <i class="bi bi-exclamation-triangle-fill me-2"></i>
                <%= error %>
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        </div>
    <% } %>

    <!-- Main Content -->
    <main class="flex-grow-1">
        <div class="container py-5">
            <!-- Dashboard Header -->
            <div class="row mb-5">
                <div class="col">
                    <div class="card border-0 shadow-sm bg-gradient text-dark" style="background: linear-gradient(135deg, #254aec 0%, #3a0ca3 100%);">
                        <div class="card-body p-4">
                            <div class="row align-items-center">
                                <div class="col-md-8">
                                    <h2 class="fw-bold mb-2">
                                        <i class="bi bi-speedometer2 me-2"></i>
                                        Welcome, <%= user.name %>!
                                    </h2>
                                    <p class="mb-0 opacity-75">
                                        Manage your events, registrations, and wishlist from one place.
                                    </p>
                                    <% if (totalSpent && totalSpent > 0) { %>
                                        <div class="mt-3">
                                            <span class="badge bg-white text-dark p-2">
                                                <i class="bi bi-wallet2 me-1"></i>
                                                Total Spent: $<%= totalSpent.toFixed(2) %>
                                            </span>
                                        </div>
                                    <% } %>
                                </div>
                                <div class="col-md-4 text-end">
                                    <% if (user.role === 'organizer') { %>
                                        <a href="/events/create" class="btn btn-light btn-lg">
                                            <i class="bi bi-plus-circle me-2"></i>Create Event
                                        </a>
                                    <% } %>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Stats Cards -->
            <div class="row g-4 mb-5">
                <div class="col-md-3">
                    <div class="card border-0 shadow-sm h-100">
                        <div class="card-body p-4">
                            <div class="d-flex align-items-center">
                                <div class="rounded-circle bg-primary bg-opacity-10 p-3 me-3">
                                    <i class="bi bi-calendar3 text-primary fs-3"></i>
                                </div>
                                <div>
                                    <h3 class="fw-bold mb-0"><%= stats.eventsAttended || 0 %></h3>
                                    <p class="text-muted mb-0">Events Attended</p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                
                <div class="col-md-3">
                    <div class="card border-0 shadow-sm h-100">
                        <div class="card-body p-4">
                            <div class="d-flex align-items-center">
                                <div class="rounded-circle bg-success bg-opacity-10 p-3 me-3">
                                    <i class="bi bi-ticket-perforated text-success fs-3"></i>
                                </div>
                                <div>
                                    <h3 class="fw-bold mb-0"><%= stats.upcomingEvents || 0 %></h3>
                                    <p class="text-muted mb-0">Upcoming Events</p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                
                <div class="col-md-3">
                    <div class="card border-0 shadow-sm h-100">
                        <div class="card-body p-4">
                            <div class="d-flex align-items-center">
                                <div class="rounded-circle bg-warning bg-opacity-10 p-3 me-3">
                                    <i class="bi bi-heart text-warning fs-3"></i>
                                </div>
                                <div>
                                    <h3 class="fw-bold mb-0"><%= stats.wishlistedEvents || 0 %></h3>
                                    <p class="text-muted mb-0">Wishlisted Events</p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                
                <div class="col-md-3">
                    <div class="card border-0 shadow-sm h-100">
                        <div class="card-body p-4">
                            <div class="d-flex align-items-center">
                                <div class="rounded-circle bg-info bg-opacity-10 p-3 me-3">
                                    <i class="bi bi-star text-info fs-3"></i>
                                </div>
                                <div>
                                    <h3 class="fw-bold mb-0"><%= stats.reviewsGiven || 0 %></h3>
                                    <p class="text-muted mb-0">Reviews Given</p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Recent Tickets Section -->
            <div class="row mb-5">
                <div class="col-12">
                    <div class="card border-0 shadow-sm">
                        <div class="card-header bg-white border-0 py-3">
                            <div class="d-flex justify-content-between align-items-center">
                                <h5 class="fw-bold mb-0">
                                    <i class="bi bi-ticket-perforated text-success me-2"></i>
                                    Recent Tickets (Confirmed Payments)
                                </h5>
                                <% if (recentTickets && recentTickets.length > 0) { %>
                                    <a href="/users/tickets" class="btn btn-sm btn-primary">
                                        <i class="bi bi-arrow-right me-1"></i>View All
                                    </a>
                                <% } %>
                            </div>
                        </div>
                        
                        <div class="card-body p-4">
                            <% if (recentTickets && recentTickets.length > 0) { %>
                                <div class="table-responsive">
                                    <table class="table table-hover align-middle mb-0">
                                        <thead class="table-light">
                                            <tr>
                                                <th>Event</th>
                                                <th>Date</th>
                                                <th>Tickets</th>
                                                <th>Amount</th>
                                                <th>Status</th>
                                                <th>Action</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            <% recentTickets.forEach(ticket => { 
                                                const event = ticket.event;
                                                if (!event) return;
                                                const eventDate = new Date(event.date);
                                            %>
                                                <tr class="recent-ticket-item">
                                                    <td>
                                                        <div class="d-flex align-items-center">
                                                            <% if (event.imageUrl && event.imageUrl !== '/images/default-event.jpg') { %>
                                                                <img src="<%= event.imageUrl %>" 
                                                                     alt="<%= event.title %>"
                                                                     class="rounded me-3"
                                                                     style="width: 50px; height: 50px; object-fit: cover;">
                                                            <% } else { %>
                                                                <div class="rounded bg-primary bg-opacity-10 d-flex align-items-center justify-content-center me-3"
                                                                     style="width: 50px; height: 50px;">
                                                                    <i class="bi bi-calendar-event text-primary"></i>
                                                                </div>
                                                            <% } %>
                                                            <div>
                                                                <h6 class="fw-bold mb-0"><%= event.title %></h6>
                                                                <small class="text-muted d-block">
                                                                    <i class="bi bi-geo-alt me-1"></i><%= event.city || 'N/A' %>
                                                                </small>
                                                            </div>
                                                        </div>
                                                    </td>
                                                    <td>
                                                        <div class="fw-semibold">
                                                            <%= eventDate.toLocaleDateString('en-US', { 
                                                                month: 'short', 
                                                                day: 'numeric' 
                                                            }) %>
                                                        </div>
                                                        <small class="text-muted">
                                                            <%= event.time || 'N/A' %>
                                                        </small>
                                                    </td>
                                                    <td>
                                                        <span class="badge bg-primary bg-opacity-10 text-primary">
                                                            <i class="bi bi-ticket-perforated me-1"></i>
                                                            <%= ticket.ticketQuantity %>
                                                        </span>
                                                    </td>
                                                    <td>
                                                        <span class="badge bg-success bg-opacity-10 text-success amount-badge">
                                                            $<%= parseFloat(ticket.totalAmount || 0).toFixed(2) %>
                                                        </span>
                                                    </td>
                                                    <td>
                                                        <span class="badge bg-success payment-status-badge">
                                                            <i class="bi bi-check-circle me-1"></i>
                                                            <%= ticket.paymentStatus || 'completed' %>
                                                        </span>
                                                    </td>
                                                    <td>
                                                        <a href="/users/registrations/<%= ticket.registrationId %>" 
                                                           class="btn btn-sm btn-outline-primary">
                                                            <i class="bi bi-eye me-1"></i>View
                                                        </a>
                                                    </td>
                                                </tr>
                                            <% }) %>
                                        </tbody>
                                    </table>
                                </div>
                                
                                <% if (totalSpent && totalSpent > 0) { %>
                                    <div class="mt-4 pt-3 border-top">
                                        <div class="d-flex justify-content-between align-items-center">
                                            <span class="text-muted">Total spent on tickets:</span>
                                            <span class="fw-bold text-success fs-5">$<%= totalSpent.toFixed(2) %></span>
                                        </div>
                                    </div>
                                <% } %>
                                
                                <div class="text-center mt-4">
                                    <a href="/users/tickets" class="btn btn-primary">
                                        <i class="bi bi-ticket-perforated me-2"></i>View All Tickets
                                    </a>
                                </div>
                            <% } else { %>
                                <div class="text-center py-5">
                                    <div class="mb-4">
                                        <i class="bi bi-ticket-perforated text-muted" style="font-size: 4rem;"></i>
                                    </div>
                                    <h5 class="fw-bold mb-3">No recent tickets</h5>
                                    <p class="text-muted mb-4">
                                        You haven't purchased any event tickets yet. Browse events and make your first booking!
                                    </p>
                                    <a href="/events" class="btn btn-primary btn-lg">
                                        <i class="bi bi-search me-2"></i>Browse Events
                                    </a>
                                </div>
                            <% } %>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Tabs Section -->
            <div class="row">
                <div class="col-lg-8">
                    <div class="card border-0 shadow-sm mb-4">
                        <div class="card-header bg-white border-0 py-3">
                            <ul class="nav nav-tabs nav-tabs-custom border-0" id="dashboardTabs" role="tablist">
                                <li class="nav-item" role="presentation">
                                    <button class="nav-link active" id="upcoming-tab" data-bs-toggle="tab" data-bs-target="#upcoming" type="button" role="tab">
                                        <i class="bi bi-calendar-event me-2"></i>Upcoming Events
                                    </button>
                                </li>
                                <li class="nav-item" role="presentation">
                                    <button class="nav-link" id="past-tab" data-bs-toggle="tab" data-bs-target="#past" type="button" role="tab">
                                        <i class="bi bi-clock-history me-2"></i>Past Events
                                    </button>
                                </li>
                                <% if (user.role === 'organizer') { %>
                                    <li class="nav-item" role="presentation">
                                        <button class="nav-link" id="myevents-tab" data-bs-toggle="tab" data-bs-target="#myevents" type="button" role="tab">
                                            <i class="bi bi-person-workspace me-2"></i>My Events
                                        </button>
                                    </li>
                                <% } %>
                            </ul>
                        </div>
                        
                        <div class="card-body p-4">
                            <div class="tab-content" id="dashboardTabsContent">
                                <!-- Upcoming Events Tab -->
                                <div class="tab-pane fade show active" id="upcoming" role="tabpanel">
                                    <h5 class="fw-bold mb-4">Your Upcoming Registrations</h5>
                                    <% if (upcomingRegistrations && upcomingRegistrations.length > 0) { %>
                                        <div class="list-group list-group-flush">
                                            <% upcomingRegistrations.forEach(reg => { 
                                                const event = reg.event;
                                                if (!event) return;
                                            %>
                                                <div class="list-group-item border-0 px-0 py-3">
                                                    <div class="row align-items-center">
                                                        <div class="col-auto">
                                                            <div class="event-icon bg-primary bg-opacity-10">
                                                                <% if (event.category === 'concert') { %>
                                                                    <i class="bi bi-music-note-beamed text-primary fs-4"></i>
                                                                <% } else if (event.category === 'conference') { %>
                                                                    <i class="bi bi-laptop text-primary fs-4"></i>
                                                                <% } else if (event.category === 'workshop') { %>
                                                                    <i class="bi bi-tools text-primary fs-4"></i>
                                                                <% } else if (event.category === 'sports') { %>
                                                                    <i class="bi bi-trophy text-primary fs-4"></i>
                                                                <% } else if (event.category === 'festival') { %>
                                                                    <i class="bi bi-emoji-sunglasses text-primary fs-4"></i>
                                                                <% } else { %>
                                                                    <i class="bi bi-calendar-event text-primary fs-4"></i>
                                                                <% } %>
                                                            </div>
                                                        </div>
                                                        <div class="col">
                                                            <h6 class="fw-bold mb-1"><%= event.title %></h6>
                                                            <p class="text-muted small mb-1">
                                                                <i class="bi bi-calendar me-1"></i>
                                                                <%= new Date(event.date).toLocaleDateString() %>
                                                                <% if (event.venue) { %>
                                                                    <i class="bi bi-geo-alt ms-3 me-1"></i>
                                                                    <%= event.venue %>
                                                                <% } %>
                                                            </p>
                                                            <div class="badge bg-success bg-opacity-10 text-success">
                                                                <i class="bi bi-check-circle me-1"></i>Confirmed
                                                            </div>
                                                        </div>
                                                        <div class="col-auto">
                                                            <a href="/users/registrations/<%= reg.registrationId %>" class="btn btn-outline-primary btn-sm">
                                                                <i class="bi bi-ticket-perforated me-1"></i>View Ticket
                                                            </a>
                                                        </div>
                                                    </div>
                                                </div>
                                            <% }) %>
                                        </div>
                                    <% } else { %>
                                        <div class="text-center py-5">
                                            <div class="mb-3">
                                                <i class="bi bi-calendar-plus text-muted fs-1"></i>
                                            </div>
                                            <h5 class="fw-bold">No upcoming registrations</h5>
                                            <p class="text-muted mb-3">You haven't registered for any upcoming events yet.</p>
                                            <a href="/events" class="btn btn-primary">Browse Events</a>
                                        </div>
                                    <% } %>
                                </div>
                                
                                <!-- Past Events Tab -->
                                <div class="tab-pane fade" id="past" role="tabpanel">
                                    <h5 class="fw-bold mb-4">Your Past Events</h5>
                                    <% if (pastRegistrations && pastRegistrations.length > 0) { %>
                                        <div class="list-group list-group-flush">
                                            <% pastRegistrations.forEach(reg => { 
                                                const event = reg.event;
                                                if (!event) return;
                                            %>
                                                <div class="list-group-item border-0 px-0 py-3">
                                                    <div class="row align-items-center">
                                                        <div class="col-auto">
                                                            <div class="event-icon bg-secondary bg-opacity-10">
                                                                <i class="bi bi-clock-history text-secondary fs-4"></i>
                                                            </div>
                                                        </div>
                                                        <div class="col">
                                                            <h6 class="fw-bold mb-1"><%= event.title %></h6>
                                                            <p class="text-muted small mb-1">
                                                                <i class="bi bi-calendar me-1"></i>
                                                                <%= new Date(event.date).toLocaleDateString() %>
                                                            </p>
                                                            <div class="badge bg-secondary bg-opacity-10 text-secondary">Completed</div>
                                                        </div>
                                                        <div class="col-auto">
                                                            <a href="/events/<%= event.eventId %>/review" class="btn btn-outline-secondary btn-sm">
                                                                <i class="bi bi-star me-1"></i>Review
                                                            </a>
                                                        </div>
                                                    </div>
                                                </div>
                                            <% }) %>
                                        </div>
                                    <% } else { %>
                                        <div class="text-center py-5">
                                            <div class="mb-3">
                                                <i class="bi bi-clock-history text-muted fs-1"></i>
                                            </div>
                                            <h5 class="fw-bold">No past events yet</h5>
                                            <p class="text-muted">Your past event history will appear here</p>
                                            <a href="/events" class="btn btn-primary">Browse Events</a>
                                        </div>
                                    <% } %>
                                </div>
                                
                                <!-- My Events Tab (Organizer) -->
                                <% if (user.role === 'organizer') { %>
                                    <div class="tab-pane fade" id="myevents" role="tabpanel">
                                        <div class="d-flex justify-content-between align-items-center mb-4">
                                            <h5 class="fw-bold mb-0">Events You Created</h5>
                                            <a href="/events/create" class="btn btn-primary btn-sm">
                                                <i class="bi bi-plus-circle me-2"></i>Create New
                                            </a>
                                        </div>
                                        
                                        <% if (myEvents && myEvents.length > 0) { %>
                                            <div class="list-group list-group-flush">
                                                <% myEvents.forEach(event => { %>
                                                    <div class="list-group-item border-0 px-0 py-3">
                                                        <div class="row align-items-center">
                                                            <div class="col-auto">
                                                                <div class="event-icon bg-info bg-opacity-10">
                                                                    <i class="bi bi-person-workspace text-info fs-4"></i>
                                                                </div>
                                                            </div>
                                                            <div class="col">
                                                                <h6 class="fw-bold mb-1"><%= event.title %></h6>
                                                                <p class="text-muted small mb-1">
                                                                    <i class="bi bi-calendar me-1"></i>
                                                                    <%= new Date(event.date).toLocaleDateString() %>
                                                                    <span class="badge bg-<%= event.status === 'upcoming' ? 'success' : event.status === 'cancelled' ? 'danger' : 'secondary' %> ms-2">
                                                                        <%= event.status %>
                                                                    </span>
                                                                </p>
                                                                <p class="text-muted small mb-0">
                                                                    <i class="bi bi-people me-1"></i>
                                                                    <%= event.availableSeats %>/<%= event.capacity %> seats available
                                                                </p>
                                                            </div>
                                                            <div class="col-auto">
                                                                <a href="/events/<%= event.eventId %>/manage" class="btn btn-outline-info btn-sm">
                                                                    <i class="bi bi-gear me-1"></i>Manage
                                                                </a>
                                                            </div>
                                                        </div>
                                                    </div>
                                                <% }) %>
                                            </div>
                                        <% } else { %>
                                            <div class="text-center py-5">
                                                <div class="mb-3">
                                                    <i class="bi bi-calendar-plus text-muted fs-1"></i>
                                                </div>
                                                <h5 class="fw-bold">No events created yet</h5>
                                                <p class="text-muted mb-3">Start organizing your first event</p>
                                                <a href="/events/create" class="btn btn-primary">Create Event</a>
                                            </div>
                                        <% } %>
                                    </div>
                                <% } %>
                            </div>
                        </div>
                    </div>
                </div>
                
                <!-- Quick Actions Sidebar -->
                <div class="col-lg-4">
                    <div class="card border-0 shadow-sm mb-4">
                        <div class="card-header bg-white border-0 py-3">
                            <h5 class="fw-bold mb-0">
                                <i class="bi bi-lightning-charge text-warning me-2"></i>Quick Actions
                            </h5>
                        </div>
                        <div class="card-body p-3">
                            <div class="d-grid gap-2">
                                <a href="/events" class="btn btn-outline-primary text-start py-3">
                                    <i class="bi bi-search me-3 fs-5"></i>
                                    <div>
                                        <h6 class="fw-bold mb-1">Browse Events</h6>
                                        <p class="text-muted small mb-0">Discover upcoming events</p>
                                    </div>
                                </a>
                                
                                <a href="/wishlist" class="btn btn-outline-warning text-start py-3">
                                    <i class="bi bi-heart me-3 fs-5"></i>
                                    <div>
                                        <h6 class="fw-bold mb-1">Wishlist</h6>
                                        <p class="text-muted small mb-0">View saved events</p>
                                    </div>
                                </a>
                                
                                <a href="/users/tickets" class="btn btn-outline-success text-start py-3">
                                    <i class="bi bi-ticket-perforated me-3 fs-5"></i>
                                    <div>
                                        <h6 class="fw-bold mb-1">My Tickets</h6>
                                        <p class="text-muted small mb-0">View all registrations</p>
                                    </div>
                                </a>
                                
                                <% if (user.role === 'organizer') { %>
                                    <a href="/events/create" class="btn btn-outline-info text-start py-3">
                                        <i class="bi bi-plus-circle me-3 fs-5"></i>
                                        <div>
                                            <h6 class="fw-bold mb-1">Create Event</h6>
                                            <p class="text-muted small mb-0">Organize a new event</p>
                                        </div>
                                    </a>
                                <% } %>
                                
                                <a href="/auth/profile" class="btn btn-outline-secondary text-start py-3">
                                    <i class="bi bi-person-circle me-3 fs-5"></i>
                                    <div>
                                        <h6 class="fw-bold mb-1">Profile Settings</h6>
                                        <p class="text-muted small mb-0">Update your account</p>
                                    </div>
                                </a>
                            </div>
                        </div>
                    </div>
                    
                    <!-- Recent Activity -->
                    <div class="card border-0 shadow-sm">
                        <div class="card-header bg-white border-0 py-3">
                            <h5 class="fw-bold mb-0">
                                <i class="bi bi-activity text-primary me-2"></i>Recent Activity
                            </h5>
                        </div>
                        <div class="card-body p-3">
                            <% if (recentActivity && recentActivity.length > 0) { %>
                                <div class="timeline">
                                    <% recentActivity.forEach(activity => { %>
                                        <div class="timeline-item">
                                            <div class="timeline-icon bg-<%= activity.color || 'primary' %>">
                                                <i class="bi bi-<%= activity.icon || 'activity' %>"></i>
                                            </div>
                                            <div class="timeline-content">
                                                <h6 class="fw-bold"><%= activity.message || 'Activity' %></h6>
                                                <p class="text-muted small mb-0">
                                                    <% if (activity.date) { 
                                                        const now = new Date();
                                                        const activityDate = new Date(activity.date);
                                                        const diffInHours = Math.floor((now - activityDate) / (1000 * 60 * 60));
                                                        
                                                        if (diffInHours < 1) {
                                                            %>Just now<%
                                                        } else if (diffInHours < 24) {
                                                            %><%= diffInHours %> hours ago<%
                                                        } else if (diffInHours < 48) {
                                                            %>Yesterday<%
                                                        } else {
                                                            %><%= Math.floor(diffInHours / 24) %> days ago<%
                                                        }
                                                    } %>
                                                </p>
                                            </div>
                                        </div>
                                    <% }) %>
                                </div>
                            <% } else { %>
                                <div class="text-center py-3">
                                    <i class="bi bi-activity text-muted fs-4 mb-2"></i>
                                    <p class="text-muted mb-0">No recent activity</p>
                                    <small class="text-muted">Your activity will appear here</small>
                                </div>
                            <% } %>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <footer class="footer py-5">
        <div class="container">
            <div class="row">
                <div class="col-md-6">
                    <h4 class="mb-3">
                        <i class="bi bi-calendar-event"></i> EventHub
                    </h4>
                    <p class="text-light">
                        Your gateway to amazing events and unforgettable experiences.
                    </p>
                </div>
                <div class="col-md-6 text-end">
                    <p class="text-light mb-0">
                        &copy; 2024 EventHub. All rights reserved.
                    </p>
                </div>
            </div>
        </div>
    </footer>

    <!-- Bootstrap JS Bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    
    <script>
        // Initialize Bootstrap tabs
        document.addEventListener('DOMContentLoaded', function() {
            const triggerTabList = [].slice.call(document.querySelectorAll('#dashboardTabs button'))
            triggerTabList.forEach(function (triggerEl) {
                const tabTrigger = new bootstrap.Tab(triggerEl)
                
                triggerEl.addEventListener('click', function (event) {
                    event.preventDefault()
                    tabTrigger.show()
                })
            });
            
            // Auto-refresh dashboard data every 60 seconds
            setInterval(function() {
                // Simple refresh without notification
                console.log('Dashboard data auto-refresh at:', new Date().toLocaleTimeString());
            }, 60000);
        });
    </script>
</body>
</html>