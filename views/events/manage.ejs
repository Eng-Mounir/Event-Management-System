<!DOCTYPE html>
<html lang="en" data-bs-theme="light">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | EventHub</title>
    
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
    
    <!-- DataTables CSS -->
    <link rel="stylesheet" href="https://cdn.datatables.net/1.13.6/css/dataTables.bootstrap5.min.css">
    
    <style>
        :root {
            --primary: #4361ee;
            --secondary: #3a0ca3;
            --gradient: linear-gradient(135deg, #4361ee 0%, #3a0ca3 100%);
        }
        
        body {
            font-family: 'Inter', sans-serif;
            background-color: #f8fafc;
            min-height: 100vh;
        }
        
        .navbar-brand {
            font-family: 'Poppins', sans-serif;
            font-weight: 700;
            font-size: 1.8rem;
            background: var(--gradient);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }
        
        .card {
            border: none;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
            margin-bottom: 20px;
        }
        
        .btn-primary {
            background: var(--gradient);
            border: none;
            padding: 12px 30px;
            border-radius: 50px;
        }
        
        .btn-primary:hover {
            opacity: 0.9;
        }
        
        .status-badge {
            font-size: 0.75rem;
            padding: 0.3rem 0.75rem;
            border-radius: 50px;
        }
        
        .stat-card {
            text-align: center;
            padding: 1.5rem;
        }
        
        .stat-number {
            font-size: 2rem;
            font-weight: 700;
            margin-bottom: 0.5rem;
        }
        
        .timeline-item {
            padding: 0.5rem 0;
            border-left: 2px solid #dee2e6;
            padding-left: 1rem;
            margin-left: 0.5rem;
        }
        
        .timeline-item.current {
            border-left-color: var(--primary);
            background-color: rgba(67, 97, 238, 0.05);
        }
        
        .timeline-time {
            font-size: 0.75rem;
            color: #6c757d;
        }
        
        /* Navigation Tabs */
        .nav-tabs-custom .nav-link {
            border: none;
            color: #6c757d;
            font-weight: 500;
            padding: 0.75rem 1.5rem;
            border-bottom: 3px solid transparent;
        }
        
        .nav-tabs-custom .nav-link.active {
            color: #4361ee;
            border-bottom: 3px solid #4361ee;
            background: transparent;
        }
        
        /* Memento Controls */
        .memento-controls {
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
            border-radius: 10px;
            padding: 1rem;
            margin-bottom: 1rem;
        }
        
        /* Registration Table */
        .attendee-row:hover {
            background-color: rgba(67, 97, 238, 0.05);
        }
        
        /* Review Stars */
        .star-rating {
            color: #ffc107;
        }
        
        /* Progress Bar */
        .progress {
            height: 8px;
            border-radius: 4px;
        }
    </style>
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-light bg-white sticky-top shadow-sm">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="bi bi-calendar-event me-2"></i>EventHub
            </a>
            
            <div class="collapse navbar-collapse">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/dashboard">
                            <i class="bi bi-speedometer2"></i> Dashboard
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/events">
                            <i class="bi bi-calendar3"></i> Events
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="/events/<%= event.eventId %>/manage">
                            <i class="bi bi-gear"></i> Manage Event
                        </a>
                    </li>
                </ul>
                
                <div class="d-flex align-items-center">
                    <div class="dropdown">
                        <a href="#" class="d-flex align-items-center text-decoration-none dropdown-toggle" 
                           data-bs-toggle="dropdown">
                            <div class="rounded-circle bg-primary d-flex align-items-center justify-content-center" 
                                 style="width: 40px; height: 40px; color: white; font-weight: 600;">
                                <%= user.name.charAt(0).toUpperCase() %>
                            </div>
                            <span class="ms-2"><%= user.name %></span>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end">
                            <li><a class="dropdown-item" href="/dashboard">
                                <i class="bi bi-speedometer2 me-2"></i>Dashboard
                            </a></li>
                            <li><a class="dropdown-item" href="/auth/profile">
                                <i class="bi bi-person-circle me-2"></i>Profile
                            </a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="/auth/logout">
                                <i class="bi bi-box-arrow-right me-2"></i>Logout
                            </a></li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </nav>

    <!-- Flash Messages -->
    <% if (success) { %>
        <div class="container mt-3">
            <div class="alert alert-success alert-dismissible fade show" role="alert">
                <i class="bi bi-check-circle-fill me-2"></i>
                <%= success %>
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        </div>
    <% } %>
    
    <% if (error) { %>
        <div class="container mt-3">
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                <i class="bi bi-exclamation-triangle-fill me-2"></i>
                <%= error %>
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        </div>
    <% } %>

    <!-- Main Content -->
    <main>
        <div class="container py-4">
            <!-- Event Header -->
            <div class="row mb-4">
                <div class="col">
                    <div class="card">
                        <div class="card-body p-4">
                            <div class="row align-items-center">
                                <div class="col-md-2">
                                    <% if (event.imageUrl && event.imageUrl !== '/images/default-event.jpg') { %>
                                        <img src="<%= event.imageUrl %>" 
                                             alt="<%= event.title %>"
                                             class="img-fluid rounded"
                                             style="height: 120px; width: 100%; object-fit: cover;">
                                    <% } else { %>
                                        <div class="rounded bg-primary bg-opacity-10 d-flex align-items-center justify-content-center"
                                             style="height: 120px;">
                                            <i class="bi bi-calendar-event text-primary" style="font-size: 3rem;"></i>
                                        </div>
                                    <% } %>
                                </div>
                                <div class="col-md-6">
                                    <h2 class="fw-bold mb-1"><%= event.title %></h2>
                                    <p class="text-muted mb-2">
                                        <i class="bi bi-calendar me-1"></i>
                                        <%= new Date(event.date).toLocaleDateString('en-US', { 
                                            weekday: 'long', 
                                            year: 'numeric', 
                                            month: 'long', 
                                            day: 'numeric' 
                                        }) %>
                                        at <%= event.time %>
                                    </p>
                                    <p class="mb-0">
                                        <i class="bi bi-geo-alt me-1"></i>
                                        <%= event.venue %>, <%= event.city %>
                                    </p>
                                </div>
                                <div class="col-md-4">
                                    <div class="d-flex gap-2 mb-3">
                                        <span class="status-badge bg-<%= event.status === 'upcoming' ? 'success' : event.status === 'cancelled' ? 'danger' : 'secondary' %>">
                                            <%= event.status.toUpperCase() %>
                                        </span>
                                        <span class="status-badge bg-primary">
                                            <%= event.category.toUpperCase() %>
                                        </span>
                                    </div>
                                    <div class="d-flex gap-2">
                                        <a href="/events/<%= event.eventId %>/edit" class="btn btn-outline-primary">
                                            <i class="bi bi-pencil"></i> Edit
                                        </a>
                                        <a href="/events/<%= event.eventId %>" class="btn btn-outline-secondary">
                                            <i class="bi bi-eye"></i> View
                                        </a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Memento History Controls -->
            <div class="row mb-4">
                <div class="col">
                    <div class="memento-controls">
                        <div class="d-flex justify-content-between align-items-center">
                            <div>
                                <h5 class="fw-bold mb-2">
                                    <i class="bi bi-clock-history me-2"></i>Edit History
                                    <small class="text-muted ms-2">(Memento Pattern)</small>
                                </h5>
                            </div>
                            <div class="d-flex gap-2">
                                <form action="/events/<%= event.eventId %>/undo" method="POST" style="display: inline;">
                                    <button type="submit" class="btn btn-outline-dark <%= !canUndo ? 'disabled' : '' %>"
                                            <%= !canUndo ? 'disabled' : '' %>>
                                        <i class="bi bi-arrow-counterclockwise"></i> Undo
                                    </button>
                                </form>
                                <form action="/events/<%= event.eventId %>/redo" method="POST" style="display: inline;">
                                    <button type="submit" class="btn btn-outline-dark <%= !canRedo ? 'disabled' : '' %>"
                                            <%= !canRedo ? 'disabled' : '' %>>
                                        <i class="bi bi-arrow-clockwise"></i> Redo
                                    </button>
                                </form>
                                <form action="/events/<%= event.eventId %>/clear-history" method="POST" style="display: inline;">
                                    <button type="submit" class="btn btn-outline-danger" 
                                            onclick="return confirm('Are you sure you want to clear all edit history? This cannot be undone.')">
                                        <i class="bi bi-trash"></i> Clear History
                                    </button>
                                </form>
                            </div>
                        </div>
                        
                        <% if (mementoHistory && mementoHistory.length > 0) { %>
                            <div class="mt-3">
                                <h6 class="fw-bold mb-2">Recent Changes:</h6>
                                <div class="timeline">
                                    <% mementoHistory.forEach((history, index) => { %>
                                        <div class="timeline-item <%= history.isCurrent ? 'current' : '' %>">
                                            <div class="d-flex justify-content-between">
                                                <span>
                                                    <i class="bi bi-circle-fill me-2" style="font-size: 0.5rem;"></i>
                                                    Version <%= index + 1 %>
                                                </span>
                                                <span class="timeline-time">
                                                    <%= new Date(history.timestamp).toLocaleString() %>
                                                </span>
                                            </div>
                                        </div>
                                    <% }) %>
                                </div>
                            </div>
                        <% } else { %>
                            <p class="text-muted mb-0 mt-2">
                                <i class="bi bi-info-circle me-1"></i>
                                No edit history yet. Changes will be recorded here.
                            </p>
                        <% } %>
                    </div>
                </div>
            </div>

            <!-- Stats Cards -->
            <div class="row mb-4">
                <div class="col-md-3">
                    <div class="card stat-card">
                        <div class="card-body">
                            <div class="stat-number text-primary">
                                <%= stats.totalRegistrations %>
                            </div>
                            <h6 class="text-muted">Total Registrations</h6>
                        </div>
                    </div>
                </div>
                <div class="col-md-3">
                    <div class="card stat-card">
                        <div class="card-body">
                            <div class="stat-number text-success">
                                $<%= stats.totalRevenue %>
                            </div>
                            <h6 class="text-muted">Total Revenue</h6>
                        </div>
                    </div>
                </div>
                <div class="col-md-3">
                    <div class="card stat-card">
                        <div class="card-body">
                            <div class="stat-number text-warning">
                                <%= stats.averageRating.toFixed(1) %>/5.0
                            </div>
                            <h6 class="text-muted">Avg Rating (<%= stats.totalReviews %>)</h6>
                        </div>
                    </div>
                </div>
                <div class="col-md-3">
                    <div class="card stat-card">
                        <div class="card-body">
                            <div class="stat-number text-info">
                                <%= stats.fillPercentage %>%
                            </div>
                            <div class="progress mt-2">
                                <div class="progress-bar bg-info" 
                                     role="progressbar" 
                                     style="width: <%= stats.fillPercentage %>%"
                                     aria-valuenow="<%= stats.fillPercentage %>" 
                                     aria-valuemin="0" 
                                     aria-valuemax="100">
                                </div>
                            </div>
                            <h6 class="text-muted mt-2">
                                <%= stats.seatsFilled %>/<%= event.capacity %> Seats Filled
                            </h6>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Management Tabs -->
            <div class="card">
                <div class="card-header bg-white border-0 py-3">
                    <ul class="nav nav-tabs nav-tabs-custom border-0" id="manageTabs" role="tablist">
                        <li class="nav-item" role="presentation">
                            <button class="nav-link active" id="attendees-tab" data-bs-toggle="tab" data-bs-target="#attendees" type="button" role="tab">
                                <i class="bi bi-people me-2"></i>Attendees (<%= stats.confirmedRegistrations %>)
                            </button>
                        </li>
                        <li class="nav-item" role="presentation">
                            <button class="nav-link" id="reviews-tab" data-bs-toggle="tab" data-bs-target="#reviews" type="button" role="tab">
                                <i class="bi bi-star me-2"></i>Reviews (<%= stats.totalReviews %>)
                            </button>
                        </li>
                        <li class="nav-item" role="presentation">
                            <button class="nav-link" id="actions-tab" data-bs-toggle="tab" data-bs-target="#actions" type="button" role="tab">
                                <i class="bi bi-gear me-2"></i>Quick Actions
                            </button>
                        </li>
                    </ul>
                </div>
                
                <div class="card-body p-4">
                    <div class="tab-content" id="manageTabsContent">
                        <!-- Attendees Tab -->
                        <div class="tab-pane fade show active" id="attendees" role="tabpanel">
                            <div class="d-flex justify-content-between align-items-center mb-4">
                                <h5 class="fw-bold mb-0">Event Attendees</h5>
                                <div class="d-flex gap-2">
                                    <form action="/events/<%= event.eventId %>/export-attendees" method="GET" style="display: inline;">
                                        <button type="submit" class="btn btn-success">
                                            <i class="bi bi-download me-1"></i>Export CSV
                                        </button>
                                    </form>
                                </div>
                            </div>
                            
                            <% if (registrations && registrations.length > 0) { %>
                                <div class="table-responsive">
                                    <table class="table table-hover" id="attendeesTable">
                                        <thead class="table-light">
                                            <tr>
                                                <th>Name</th>
                                                <th>Email</th>
                                                <th>Tickets</th>
                                                <th>Amount</th>
                                                <th>Booking Date</th>
                                                <th>Status</th>
                                                <th>Actions</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            <% registrations.forEach(registration => { 
                                                const attendee = registration.user;
                                            %>
                                                <tr class="attendee-row">
                                                    <td>
                                                        <strong><%= attendee.name %></strong>
                                                    </td>
                                                    <td><%= attendee.email %></td>
                                                    <td>
                                                        <span class="badge bg-primary">
                                                            <%= registration.ticketQuantity %>
                                                        </span>
                                                    </td>
                                                    <td>
                                                        <span class="badge bg-success">
                                                            $<%= parseFloat(registration.totalAmount || 0).toFixed(2) %>
                                                        </span>
                                                    </td>
                                                    <td>
                                                        <%= new Date(registration.bookingDate).toLocaleDateString() %>
                                                    </td>
                                                    <td>
                                                        <span class="badge bg-<%= registration.status === 'confirmed' ? 'success' : 'warning' %>">
                                                            <%= registration.status %>
                                                        </span>
                                                    </td>
                                                    <td>
                                                        <button class="btn btn-sm btn-outline-primary" 
                                                                onclick="viewRegistration(<%= registration.registrationId %>)">
                                                            <i class="bi bi-eye"></i>
                                                        </button>
                                                        <% if (registration.status === 'pending') { %>
                                                            <form action="/users/registrations/<%= registration.registrationId %>/confirm" 
                                                                  method="POST" 
                                                                  style="display: inline;">
                                                                <button type="submit" class="btn btn-sm btn-success">
                                                                    <i class="bi bi-check"></i>
                                                                </button>
                                                            </form>
                                                        <% } %>
                                                    </td>
                                                </tr>
                                            <% }) %>
                                        </tbody>
                                    </table>
                                </div>
                            <% } else { %>
                                <div class="text-center py-5">
                                    <i class="bi bi-people text-muted" style="font-size: 3rem;"></i>
                                    <h5 class="fw-bold mt-3">No attendees yet</h5>
                                    <p class="text-muted">Attendees will appear here once they register.</p>
                                </div>
                            <% } %>
                        </div>
                        
                        <!-- Reviews Tab -->
                        <div class="tab-pane fade" id="reviews" role="tabpanel">
                            <h5 class="fw-bold mb-4">Event Reviews</h5>
                            
                            <% if (reviews && reviews.length > 0) { %>
                                <div class="row">
                                    <div class="col-md-4 mb-4">
                                        <div class="card">
                                            <div class="card-body text-center">
                                                <h1 class="display-4 fw-bold text-warning">
                                                    <%= stats.averageRating.toFixed(1) %>
                                                </h1>
                                                <div class="star-rating mb-2">
                                                    <% for (let i = 1; i <= 5; i++) { %>
                                                        <% if (i <= Math.floor(stats.averageRating)) { %>
                                                            <i class="bi bi-star-fill"></i>
                                                        <% } else if (i === Math.ceil(stats.averageRating) && stats.averageRating % 1 !== 0) { %>
                                                            <i class="bi bi-star-half"></i>
                                                        <% } else { %>
                                                            <i class="bi bi-star"></i>
                                                        <% } %>
                                                    <% } %>
                                                </div>
                                                <p class="text-muted">
                                                    Based on <%= stats.totalReviews %> reviews
                                                </p>
                                            </div>
                                        </div>
                                    </div>
                                    <div class="col-md-8">
                                        <% reviews.forEach(review => { %>
                                            <div class="card mb-3">
                                                <div class="card-body">
                                                    <div class="d-flex justify-content-between align-items-start mb-2">
                                                        <div>
                                                            <h6 class="fw-bold mb-1"><%= review.user.name %></h6>
                                                            <div class="star-rating">
                                                                <% for (let i = 1; i <= 5; i++) { %>
                                                                    <% if (i <= review.rating) { %>
                                                                        <i class="bi bi-star-fill"></i>
                                                                    <% } else { %>
                                                                        <i class="bi bi-star"></i>
                                                                    <% } %>
                                                                <% } %>
                                                            </div>
                                                        </div>
                                                        <small class="text-muted">
                                                            <%= new Date(review.reviewDate).toLocaleDateString() %>
                                                        </small>
                                                    </div>
                                                    <% if (review.comment) { %>
                                                        <p class="mb-0"><%= review.comment %></p>
                                                    <% } else { %>
                                                        <p class="text-muted mb-0"><em>No comment provided</em></p>
                                                    <% } %>
                                                </div>
                                            </div>
                                        <% }) %>
                                    </div>
                                </div>
                            <% } else { %>
                                <div class="text-center py-5">
                                    <i class="bi bi-star text-muted" style="font-size: 3rem;"></i>
                                    <h5 class="fw-bold mt-3">No reviews yet</h5>
                                    <p class="text-muted">Reviews will appear here after attendees submit them.</p>
                                </div>
                            <% } %>
                        </div>
                        
                        <!-- Actions Tab -->
                        <div class="tab-pane fade" id="actions" role="tabpanel">
                            <h5 class="fw-bold mb-4">Event Management Actions</h5>
                            
                            <div class="row g-4">
                                <!-- Edit Event -->
                                <div class="col-md-6">
                                    <div class="card h-100 border-primary border-2">
                                        <div class="card-body">
                                            <div class="d-flex align-items-center mb-3">
                                                <div class="rounded-circle bg-primary bg-opacity-10 p-3 me-3">
                                                    <i class="bi bi-pencil-square text-primary fs-3"></i>
                                                </div>
                                                <h5 class="fw-bold mb-0">Edit Event Details</h5>
                                            </div>
                                            <p class="text-muted mb-4">
                                                Update event title, description, date, venue, capacity, and other details.
                                            </p>
                                            <a href="/events/<%= event.eventId %>/edit" class="btn btn-primary w-100">
                                                <i class="bi bi-pencil me-2"></i>Edit Event
                                            </a>
                                        </div>
                                    </div>
                                </div>
                                
                                <!-- Cancel/Reactivate -->
                                <div class="col-md-6">
                                    <div class="card h-100 <%= event.status === 'cancelled' ? 'border-success' : 'border-danger' %> border-2">
                                        <div class="card-body">
                                            <div class="d-flex align-items-center mb-3">
                                                <div class="rounded-circle <%= event.status === 'cancelled' ? 'bg-success' : 'bg-danger' %> bg-opacity-10 p-3 me-3">
                                                    <i class="bi <%= event.status === 'cancelled' ? 'bi-play-circle' : 'bi-x-circle' %> <%= event.status === 'cancelled' ? 'text-success' : 'text-danger' %> fs-3"></i>
                                                </div>
                                                <h5 class="fw-bold mb-0">
                                                    <%= event.status === 'cancelled' ? 'Reactivate' : 'Cancel' %> Event
                                                </h5>
                                            </div>
                                            <p class="text-muted mb-4">
                                                <%= event.status === 'cancelled' 
                                                    ? 'Reactivate this event to accept new registrations.' 
                                                    : 'Cancel this event. Existing registrations will be notified.' 
                                                %>
                                            </p>
                                            <form action="/events/<%= event.eventId %>/toggle-status" method="POST">
                                                <button type="submit" 
                                                        class="btn <%= event.status === 'cancelled' ? 'btn-success' : 'btn-danger' %> w-100"
                                                        onclick="return confirm('Are you sure you want to <%= event.status === 'cancelled' ? 'reactivate' : 'cancel' %> this event?')">
                                                    <i class="bi <%= event.status === 'cancelled' ? 'bi-play-circle' : 'bi-x-circle' %> me-2"></i>
                                                    <%= event.status === 'cancelled' ? 'Reactivate Event' : 'Cancel Event' %>
                                                </button>
                                            </form>
                                        </div>
                                    </div>
                                </div>
                                
                                <!-- Delete Event -->
                                <div class="col-md-6">
                                    <div class="card h-100 border-danger border-2">
                                        <div class="card-body">
                                            <div class="d-flex align-items-center mb-3">
                                                <div class="rounded-circle bg-danger bg-opacity-10 p-3 me-3">
                                                    <i class="bi bi-trash text-danger fs-3"></i>
                                                </div>
                                                <h5 class="fw-bold mb-0">Delete Event</h5>
                                            </div>
                                            <p class="text-muted mb-4">
                                                <strong>Warning:</strong> This will permanently delete the event and all associated data.
                                                <% if (stats.totalRegistrations > 0) { %>
                                                    <br><span class="text-danger">Cannot delete with active registrations.</span>
                                                <% } %>
                                            </p>
                                            <form action="/events/<%= event.eventId %>" method="POST">
                                                <input type="hidden" name="_method" value="DELETE">
                                                <button type="submit" 
                                                        class="btn btn-danger w-100 <%= stats.totalRegistrations > 0 ? 'disabled' : '' %>"
                                                        <%= stats.totalRegistrations > 0 ? 'disabled' : '' %>
                                                        onclick="return confirm('⚠️ WARNING: This will permanently delete the event and all data.\n\nAre you absolutely sure?')">
                                                    <i class="bi bi-trash me-2"></i>
                                                    Delete Event
                                                </button>
                                            </form>
                                        </div>
                                    </div>
                                </div>
                                
                                <!-- Send Notifications -->
                                <div class="col-md-6">
                                    <div class="card h-100 border-info border-2">
                                        <div class="card-body">
                                            <div class="d-flex align-items-center mb-3">
                                                <div class="rounded-circle bg-info bg-opacity-10 p-3 me-3">
                                                    <i class="bi bi-bell text-info fs-3"></i>
                                                </div>
                                                <h5 class="fw-bold mb-0">Send Notification</h5>
                                            </div>
                                            <p class="text-muted mb-4">
                                                Send a custom notification to all registered attendees.
                                            </p>
                                            <button type="button" 
                                                    class="btn btn-info w-100"
                                                    data-bs-toggle="modal" 
                                                    data-bs-target="#notificationModal">
                                                <i class="bi bi-bell me-2"></i>Send Notification
                                            </button>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <!-- Notification Modal -->
    <div class="modal fade" id="notificationModal" tabindex="-1">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">Send Notification to Attendees</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <form id="notificationForm">
                        <div class="mb-3">
                            <label class="form-label">Subject</label>
                            <input type="text" class="form-control" name="subject" required>
                        </div>
                        <div class="mb-3">
                            <label class="form-label">Message</label>
                            <textarea class="form-control" name="message" rows="4" required></textarea>
                        </div>
                        <div class="mb-3">
                            <div class="form-check">
                                <input class="form-check-input" type="checkbox" name="includeEmail" id="includeEmail" checked>
                                <label class="form-check-label" for="includeEmail">
                                    Send email notification
                                </label>
                            </div>
                            <small class="text-muted">
                                This will send to <%= stats.confirmedRegistrations %> confirmed attendees
                            </small>
                        </div>
                    </form>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                    <button type="button" class="btn btn-primary" onclick="sendNotification()">
                        <i class="bi bi-send me-1"></i>Send Notification
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS Bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    
    <!-- DataTables JS -->
    <script src="https://code.jquery.com/jquery-3.7.0.min.js"></script>
    <script src="https://cdn.datatables.net/1.13.6/js/jquery.dataTables.min.js"></script>
    <script src="https://cdn.datatables.net/1.13.6/js/dataTables.bootstrap5.min.js"></script>
    
    <script>
        // Initialize DataTable
        $(document).ready(function() {
            if ($('#attendeesTable').length) {
                $('#attendeesTable').DataTable({
                    pageLength: 10,
                    order: [[4, 'desc']],
                    responsive: true
                });
            }
            
            // Initialize tabs
            const triggerTabList = [].slice.call(document.querySelectorAll('#manageTabs button'))
            triggerTabList.forEach(function (triggerEl) {
                const tabTrigger = new bootstrap.Tab(triggerEl)
                
                triggerEl.addEventListener('click', function (event) {
                    event.preventDefault()
                    tabTrigger.show()
                })
            });
        });
        
        function viewRegistration(registrationId) {
            window.open(`/users/registrations/${registrationId}`, '_blank');
        }
        
        function sendNotification() {
            const form = document.getElementById('notificationForm');
            const formData = new FormData(form);
            const data = Object.fromEntries(formData);
            
            // TODO: Implement AJAX call to send notification
            alert('Notification feature coming soon! This will send to ' + <%= stats.confirmedRegistrations %> + ' attendees.');
            
            // Close modal
            const modal = bootstrap.Modal.getInstance(document.getElementById('notificationModal'));
            modal.hide();
        }
        
        // Auto-refresh every 60 seconds if on attendees tab
        setInterval(function() {
            if (document.querySelector('#attendees-tab.active')) {
                // TODO: Implement partial refresh of attendees table
                console.log('Auto-refreshing attendees data');
            }
        }, 60000);
    </script>
</body>
</html>